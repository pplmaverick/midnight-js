name: Check PR

on:
  pull_request:
    types:
      - opened
      - edited
      - synchronize
      - reopened

permissions:
  contents: read
  pull-requests: read
  statuses: write

jobs:
  validate-pr-title:
    name: Validate PR Title
    runs-on: ubuntu-latest
    steps:
      - uses: amannn/action-semantic-pull-request@48f256284bd46cdaab1048c3721360e808335d50 # ratchet:amannn/action-semantic-pull-request@v6
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          types: |
            feat
            fix
            docs
            style
            refactor
            perf
            test
            chore
            ci
            build
            revert
          scopes: |
            release
            testkit-js
            midnight-js
            deps
            deps-dev
            config
          requireScope: false
          subjectPattern: ^(?=.*\S).+$
          subjectPatternError: |
            The subject "{subject}" found in the pull request title "{title}"
            is empty. Please provide a meaningful description after the type/scope.
          wip: true

  validate-pr-body:
    name: Validate PR Body
    runs-on: ubuntu-latest
    steps:
      - uses: actions/github-script@3a2844b7e9c422d3c10d287c895573f7108da1b3 # ratchet:actions/github-script@v9.0.0
        with:
          script: |
            const prBody = context.payload.pull_request.body || '';
            const trimmedBody = prBody.trim();

            if (!trimmedBody || trimmedBody.length === 0) {
              core.setFailed('PR body is empty. Please provide a description of your changes.');
              return;
            }

            console.log('PR body validation passed');
