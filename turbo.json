{
  "$schema": "https://turbo.build/schema.v2.json",
  "envMode": "loose",
  "globalEnv": ["COMPACTC_VERSION"],
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "outputLogs": "new-only",
      "inputs": ["src/**/*.ts", "!src/**/*.test.ts", "src/**/*.compact"],
      "outputs": ["dist/**", "build/**", "src/**/managed/**"]
    },
    "clean": {
      "cache": false
    },
    "clean-build": {
      "cache": false,
      "dependsOn": [
        "clean",
        "build"
      ]
    },
    "test": {
      "outputLogs": "new-only",
      "dependsOn": ["^build"],
      "inputs": ["src/**/*.ts", "compose.yml"],
      "outputs": ["reports/**", "coverage/**", "logs/**"]
    },
    "it": {
      "outputLogs": "new-only",
      "dependsOn": ["^build"],
      "inputs": ["src/**/*.ts", "compose.yml"],
      "outputs": ["reports/**", "coverage/**", "logs/**"]
    },
    "e2e": {
      "outputLogs": "new-only",
      "dependsOn": ["^build"],
      "inputs": ["src/**/*.ts", "compose.yml"],
      "outputs": ["reports/**", "coverage/**", "logs/**"]
    },
    "e2e-single": {
      "outputLogs": "new-only",
      "dependsOn": ["^build"],
      "inputs": ["src/**/*.ts", "compose.yml"],
      "outputs": ["reports/**", "coverage/**", "logs/**"]
    },
    "check": {
      "outputLogs": "new-only",
      "dependsOn": ["build", "test"]
    },
    "compact": {
      "cache": false
    },
    "deploy": {
      "cache": false,
      "dependsOn": ["build"]
    }
  }
}
